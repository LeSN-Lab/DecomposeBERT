# class ConcernIdentification:
#
#     @staticmethod
#     def get_output_layer(layers):
#         for layer in layers:
#             if layer.type == Layer
#     @staticmethod
#     def get_encoder_layers(layers):
#         for layer in layers:
#             if layer.type == Layer